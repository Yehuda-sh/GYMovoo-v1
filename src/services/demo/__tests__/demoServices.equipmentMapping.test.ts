// DEMO TEST REMOVED (2025-08-13)
// Keeping file to avoid task references; test is skipped.

describe.skip("demoWorkoutService equipment mapping", () => {
  it("skipped", () => {
    expect(true).toBe(true);
  });
});
